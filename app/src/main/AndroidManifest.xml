<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pixer"
        tools:targetApi="33">

        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:theme="@style/Theme.Pixer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <service android:name=".ScreenshotService" />

        <activity android:name=".ScreenshotRetriever"
            android:parentActivityName=".ScreenshotService"/>
        <activity android:name=".ObjectDetect"
            android:parentActivityName=".ScreenshotRetriever"/>
        <activity android:name=".ImageLabelling"
            android:parentActivityName=".ObjectDetect"/>
        <activity android:name=".TextExtractor"
            android:parentActivityName=".ScreenshotRetriever"/>
        <activity android:name=".CropAndConvert"
            android:parentActivityName=".ObjectDetect"/>
        <activity android:name=".SpeakText"
            android:parentActivityName=".CropAndConvert"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

</manifest>